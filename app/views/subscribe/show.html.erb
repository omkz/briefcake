<div class="w-full h-full flex justify-around items-center content-center px-3 pt-5">
  <div class="max-w-sm text-center">
    <p class="mb-6 text-gray-600">
      By subscribing we will send you an email for every new publication on
      <%= link_to @subscribe_form.name, @subscribe_form.url, class: "text-gray-600 underline hover:no-underline" %>.
    </p>

    <div class="text-left">
      <%= form_with(model: @user, url: subscribe_path, local: true, method: :post, class: "card card--light mb-8") do |form| %>
        <div class="card__content">
          <div class="sm:flex content-center items-end">
            <div class="flex-grow">
              <div class="font-medium text-gray-700 mb-2">
                Subscribe
              </div>
              <%= form.email_field :email, name: "email", class: "input", placeholder: "your@email.com" %>
            </div>
            <%= form.submit "Subscribe", class: "cursor-pointer mt-2 sm:mt-0 sm:ml-2 px-3 py-2 bg-blue-500 rounded text-white inline-block sm:w-auto w-full", style: "background-color: #{@subscribe_form.color}" %>
          </div>
          <% if @user.errors.added?(:email, "has already been taken") %>
            <div class="mt-2 text-sm text-red-500">
              You already have an account with 🍰 briefcake, please <%= link_to "sign in", new_user_session_path, class: "text-red-500 underline" %> to add this feed to your list.
            </div>
          <% elsif @user.errors.added?(:email, "is invalid") %>
            <div class="mt-2 text-sm text-red-500">
              Invalid email address
            </div>
          <% end %>
        </div>
      <% end %>
      <div class="text-center text-gray-600 text-sm">
        <p class="my-2 text-xl">&#9710;</p>
        <p>Want more info about this service? <%= link_to "Read how it works", about_rss_newsletter_path, class: "text-gray-600 underline hover:no-underline"  %>.</p>
      </div>
    </div>
  </div>
</div>



