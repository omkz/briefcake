
<div class="mx-auto max-w-2xl px-5 pt-5">
  <div class="border bg-white border-gray-300 rounded p-5 mb-5">
    <p class="mb-4">Hello <%= @resource.first_name %>,</p>

    <p class="mb-4">Someone has requested a link to change your password. You can do this through the link below.</p>

    <p class="mb-4">👉 <%= link_to "Change my password", edit_password_url(@resource, reset_password_token: @token), class: "text-blue-500" %> 👈 </p>

    <p class="mb-4">or copy and paste this link into a browser:</p>

    <p class="mb-4"><%= edit_password_url(@resource, reset_password_token: @token) %></p>

    <p>If you didn't request this, please ignore this email.</p>
  </div>

  <div class="text-center mb-5 text-sm text-gray-600 px-4">
    <p class="mb-2">You are receiving this email because you signed up at <a href="https://briefcake.com">briefcake.com</a>.</p>
  </div>
</div>
